interface:
  display_name: "Environment Gap Log"
  short_description: "Look up and record reusable environment gaps"
  default_prompt: "Use $log-environment-gaps to look up known environment gaps read-only and record only confirmed reusable limitations or workarounds."
